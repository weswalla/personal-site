// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update this to your production domain for correct canonical URLs & sitemaps.
  site: 'https://wesleyfinck.org',
});
